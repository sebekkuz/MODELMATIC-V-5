{
  "name": "@prodsim/frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "npm --prefix ../../packages/schemas install --no-audit --no-fund && npm --prefix ../../packages/schemas run build && npm --prefix ../../packages/protocol install --no-audit --no-fund && npm --prefix ../../packages/protocol run build",
    "preinstall": "node .render-helpers/patch-workspace.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@prodsim/schemas": "file:../../packages/schemas",
    "@prodsim/protocol": "file:../../packages/protocol"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.21",
    "@vitejs/plugin-react": "^4.3.2"
  }
}
