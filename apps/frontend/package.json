{
  "name": "@prodsim/frontend",
  "version": "1.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix ../../packages/schemas ci --no-audit --no-fund && npm --prefix ../../packages/schemas run build && npm --prefix ../../packages/protocol ci --no-audit --no-fund && npm --prefix ../../packages/protocol run build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.23.8",
    "@prodsim/schemas": "file:../../packages/schemas",
    "@prodsim/protocol": "file:../../packages/protocol"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "@vitejs/plugin-react": "^4.3.3"
  }
}