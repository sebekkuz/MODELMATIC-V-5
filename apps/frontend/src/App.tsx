import { useStore } from './state/store';import Visual2D from './components/Visual2D';export default function App(){const add=useStore(s=>s.addStation);return(<div className='app'><div className='sidebar'><div className='toolbar'><button className='btn' onClick={()=> add({x: Math.floor(Math.random()*10), y: Math.floor(Math.random()*4)})}>Dodaj stanowisko</button></div><p className='label'>Kliknij aby dodać elementy.</p></div><div className='canvas-wrap'><Visual2D/></div></div>)}
