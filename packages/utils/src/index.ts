export const parseHMS=(s:string)=>{const[h,m,sec]=s.split(':').map(Number);return h*3600+m*60+sec;};export const toHMS=(t:number)=>{const h=Math.floor(t/3600);const m=Math.floor((t%3600)/60);const s=Math.round(t%60);return`${h}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;};export const mps=(kmh:number)=>kmh/3.6;export const meters=(v:number,unit:'m'|'cm'|'mm')=>unit==='m'?v:unit==='cm'?v/100:v/1000;
