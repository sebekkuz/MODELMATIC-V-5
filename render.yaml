services:
  - type: web
    name: prodsim-backend
    env: node
    plan: starter
    region: frankfurt
    autoDeploy: true
    healthCheckPath: /health
    buildCommand: |
      corepack enable
      corepack prepare pnpm@9.12.0 --activate
      pnpm i --frozen-lockfile
      pnpm --filter @prodsim/backend build
    startCommand: node apps/backend/dist/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.12.2
      - key: PORT
        value: 3000
      - key: LOG_LEVEL
        value: info
      - key: CORS_ORIGIN
        # Zmień na finalny URL frontendu po pierwszym deployu (np. https://prodsim-frontend.onrender.com)
        value: https://<frontend-domain>.onrender.com

staticSites:
  - name: prodsim-frontend
    plan: starter
    region: frankfurt
    buildCommand: |
      corepack enable
      corepack prepare pnpm@9.12.0 --activate
      pnpm i --frozen-lockfile
      pnpm --filter @prodsim/frontend build
    staticPublishPath: apps/frontend/dist
    envVars:
      - key: VITE_API_HTTP
        # Zmień na finalny URL backendu po pierwszym deployu (np. https://prodsim-backend.onrender.com)
        value: https://<backend-domain>.onrender.com
      - key: VITE_API_WS
        # Ścieżka WS do backendu (np. wss://prodsim-backend.onrender.com/ws)
        value: wss://<backend-domain>.onrender.com/ws
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      # SPA fallback (React Router)
      - type: rewrite
        source: /.*
        destination: /index.html
