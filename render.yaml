services:
  - type: web
    name: prodsim-backend
    runtime: node
    plan: free
    autoDeployTrigger: commit
    healthCheckPath: /health
    buildCommand: |
      corepack enable
      corepack prepare pnpm@9.12.0 --activate
      pnpm i --frozen-lockfile
      pnpm --filter @prodsim/backend build
    startCommand: node apps/backend/dist/server.js
    envVars:
      - key: LOG_LEVEL
        value: info
      - key: CORS_ORIGIN
        # Uzupełnij po pierwszym deployu URL-em frontendu, np. https://prodsim-frontend.onrender.com
        value: https://<frontend-domain>.onrender.com

  - type: web
    name: prodsim-frontend
    runtime: static
    autoDeployTrigger: commit
    buildCommand: |
      corepack enable
      corepack prepare pnpm@9.12.0 --activate
      pnpm i --frozen-lockfile
      pnpm --filter @prodsim/frontend build
    staticPublishPath: apps/frontend/dist
    envVars:
      - key: VITE_API_HTTP
        value: https://<backend-domain>.onrender.com
      - key: VITE_API_WS
        value: wss://<backend-domain>.onrender.com/ws
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
